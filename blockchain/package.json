{
    "name": "inkless-blockchain-scripts",
    "version": "1.0.0",
    "description": "Deployment scripts for InklessRegistry",
    "scripts": {
        "node": "npx hardhat node",
        "deploy": "npx hardhat run scripts/deploy.js --network localhost"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "hardhat": "^2.22.17"
    },
    "dependencies": {
        "dotenv": "^17.2.3"
    }
}
